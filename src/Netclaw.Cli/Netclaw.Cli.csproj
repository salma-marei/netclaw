<!-- Transitional: Web SDK retained for Termina hosting route wiring. -->
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>netclaw</AssemblyName>
    <OutputType>Exe</OutputType>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <InvariantGlobalization>true</InvariantGlobalization>
    <!-- `netclaw doctor` now loads SkiaSharp's native libSkiaSharp (the image-egress
         probe). The release publish script passes this flag to every component, but
         set it here too — intrinsic to the binary, like the daemon's csproj — so a
         manual `dotnet publish` of the CLI also embeds the native lib instead of
         emitting a sidecar that the single-binary Docker copy would drop. -->
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Netclaw.Cli.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="JsonSchema.Net" />
    <PackageReference Include="ModelContextProtocol.Core" />
    <PackageReference Include="Netclaw.SkillClient" />
    <PackageReference Include="Termina" />
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" />
  </ItemGroup>

<ItemGroup>
    <ProjectReference Include="..\Netclaw.Actors\Netclaw.Actors.csproj" />
    <ProjectReference Include="..\Netclaw.Configuration\Netclaw.Configuration.csproj" />
    <ProjectReference Include="..\Netclaw.Embeddings\Netclaw.Embeddings.csproj" />
    <ProjectReference Include="..\Netclaw.Channels\Netclaw.Channels.csproj" />
    <ProjectReference Include="..\Netclaw.Channels.Slack\Netclaw.Channels.Slack.csproj" />
    <ProjectReference Include="..\Netclaw.Providers\Netclaw.Providers.csproj" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources\identity\*.md" />
  </ItemGroup>

</Project>
